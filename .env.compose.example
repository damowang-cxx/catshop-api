# Docker Compose dedicated env file.
# Use this file with:
# docker compose --env-file .env.compose up -d ...
#
# Purpose:
# Avoid Docker Compose reading application .env values that contain "$"
# (for example Argon2 hashes), which triggers interpolation warnings.

COMPOSE_PROJECT_NAME=catshop
